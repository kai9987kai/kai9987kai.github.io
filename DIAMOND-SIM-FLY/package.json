{
  "name": "diamond-sim",
  "version": "3.0.0",
  "private": true,
  "description": "Reproducible browser world-model experiments and Nexus simulation",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "check": "node scripts/check.cjs",
    "benchmark": "node scripts/benchmark.cjs",
    "benchmark:original": "node scripts/compare-original.cjs",
    "test:browser": "node scripts/browser-check.cjs",
    "serve": "node scripts/serve.cjs",
    "benchmark:connectome": "node scripts/connectome-benchmark.cjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "playwright": "1.60.0"
  }
}
