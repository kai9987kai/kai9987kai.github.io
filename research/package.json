{
  "name": "no-instructions-for-hallucinogens",
  "private": true,
  "version": "0.1.0",
  "description": "Educational neuroscience writing project (docs scaffold + sync helpers)",
  "scripts": {
    "docs:sync": "powershell -ExecutionPolicy Bypass -File scripts\\sync_docs.ps1",
    "docs:serve": "mkdocs serve",
    "docs:build": "npm run docs:sync && mkdocs build",
    "docs:dev": "npm run docs:sync && mkdocs serve"
  }
}
